<p id="notice"><%= notice %></p>
<div class="title">
  <h1>Ideas</h1>
</div>
<div class="new-idea-button">
  <%= link_to 'New Idea', new_idea_path, :class => "btn btn-primary" %>
</div>
<div class="idea-table">
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Name</th>
        <th>Description</th>
        <th>Picture</th>
        <th>Created by</th>
        <th>Status</th>
        <th colspan="4">Action</th>
      </tr>
    </thead>

    <tbody>
      <% @ideas.each do |idea| %>
        <tr>
          <td><%= idea.name %></td>
          <td><%= idea.description %></td>
          <td><%= image_tag idea.picture_url, width: '20%' if idea.picture.present? %></td>
          <td><%= idea.author_id %></td>
          <td><%= idea.status.humanize %></td>
          <td><%= link_to 'Show', idea, class: "btn btn-primary"  %></td>
          <td><%= link_to 'Edit', edit_idea_path(idea), :class => "btn btn-warning" %></td>
          <td><%= link_to 'Destroy', idea, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-danger" %></td>
          <% if idea.status.humanize == 'Approved' %>
          <td><%= link_to 'Unapprove', change_status_path(idea), :class => "btn btn-primary" %></td>
          <%else %>
          <td><%= link_to 'Approve', change_status_path(idea), :class => "btn btn-primary" %></td>
         <%end%>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
